import React from 'react';
import ReactDOM from 'react-dom';
import App from './account/App'; // Import your main App component (or the component you want to render)

ReactDOM.render(<App />, document.getElementById('root'));
